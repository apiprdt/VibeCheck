name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest

      - name: Run tests
        run: pytest tests/ -v --tb=short

      - name: Smoke test - version
        run: vibecheck --version

      - name: Smoke test - analyze Python
        run: vibecheck examples/python_bad.py

      - name: Smoke test - analyze JavaScript
        run: vibecheck examples/node_bad.js

      - name: Smoke test - analyze Go
        run: vibecheck examples/go_bad.go

      - name: Smoke test - JSON output
        run: vibecheck examples/python_bad.py --json

      - name: Smoke test - debt scan
        run: vibecheck --debt examples
