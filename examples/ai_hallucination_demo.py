def process_agent_data(data):
    """AI-generated code often assumes perfect data structures."""
    # SIGNATURE: AI Overconfidence (Nested access without .get())
    user_id = data["response"]["user"]["id"]
    print(f"Processing user {user_id}")

    try:
        # Some complex logic that might fail
        result = 10 / 0
    except Exception:    # AI loves this
        
        # Hidden comment
        
        pass   # The trap
    
    return {"status": "success"}
